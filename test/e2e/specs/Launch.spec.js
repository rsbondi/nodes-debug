import utils from '../utils'

console.log('e2e testing deferred until refactor of nodetypes, stay tuned')
// describe('Launch', function() {
//   beforeEach(utils.beforeEach)
//   afterEach(utils.afterEach)

//   it('shows the application window', async function() {
//     const isVisible = await this.app.browserWindow.isVisible()
//     expect(isVisible).to.equal(true)
//   })
//   it('shows the proper application title', async function() {
//     const title = await this.app.client.getTitle()
//     expect(title).to.equal('nodes-rpc')
//   })
//   it('loads with no errors', async function() {
//     const logs = await this.app.client.getRenderProcessLogs();
//     const errors = logs.filter(log => {
//       console.log(log.message);
//       console.log(log.source);
//       console.log(log.level);
//       return log.level == "ERROR"
//     });
//     expect(errors.length).to.equal(0)
//   })
// })
